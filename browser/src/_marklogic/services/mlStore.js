/*

Bound to dataView
 */
/*
_marklogic/services/mlStore.js
 */

define(['_marklogic/module'], function (module) {

  module.provider('mlStore', [

    // deps tbd
    function (
      // deps tbd
    ) {

      this.$get = [
        function () {
          return {
            // attachScope: function (scope) {
            //   this.scope = scope;
            // }
          };
        }
      ];

    }
  ]);
});
